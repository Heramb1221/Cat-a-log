# Rename this file to .env and fill in your key.
# NOTE: because this is a client-only setup, this key ships inside the app
# binary and can be extracted by a determined user. Do not use a key with
# billing you're not comfortable exposing. See README "Security note".
EXPO_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here
